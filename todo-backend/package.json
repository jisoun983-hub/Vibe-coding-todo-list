{
  "name": "todo-backend",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "health": "node -e \"fetch('http://localhost:5001/health').then(r=>r.text()).then(console.log).catch(e=>{console.error(e);process.exit(1)})\"",
    "db:disable-validator": "node scripts/disable-mongo-validator.js"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongoose": "^8.18.0"
  }
}

